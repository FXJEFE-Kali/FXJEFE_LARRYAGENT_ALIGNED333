# ⚡ SkillManager Quick Reference (Cheat Sheet)\n**Agent-Larry v2.1 | For Developers**\n\n---\n\n## 🧰 Core API (30 seconds)\n\n```python\nfrom agent_v2 import AgentLarry\n\nagent = AgentLarry()\n\n# Register a skill at startup\nagent.skill_manager.register_skill(\n    name=\"my_skill\",\n    func=lambda **kw: {\"success\": True, \"result\": \"Done\"},\n    description=\"Do something cool\",\n    category=\"my_category\"\n)\n\n# Run a skill\nresult = agent.skill_manager.execute_skill(\"my_skill\")\nprint(result[\"result\"])  # \"Done\"\n\n# List all skills\nskills = agent.skill_manager.get_all_skills()  # Dict[category, List[skill]]\n\n# Count skills\ncount = agent.skill_manager.get_skill_count()  # int\n```\n\n---\n\n## 🎯 Minimal Skill Template\n\n```python\ndef _skill_my_skill(**kwargs):\n    try:\n        # Your logic here\n        result = \"Output message\"\n        logger.info(\"my_skill: done\")\n        return {\"success\": True, \"result\": result}\n    except Exception as e:\n        logger.error(f\"my_skill: {e}\")\n        return {\"success\": False, \"error\": str(e)}\n\n# Register in _register_agent_skills():\nself.skill_manager.register_skill(\n    name=\"my_skill\",\n    func=_skill_my_skill,\n    description=\"Does something\",\n    category=\"mycategory\"\n)\n```\n\nThen:\n```bash\n/skill my_skill\n```\n\n---\n\n## 📋 All Built-in Skills\n\n| Name | Category | What it does |\n|------|----------|-------------|\n| `telegram_bot_status` | telegram | Check bot token validity |\n| `telegram_send_test` | telegram | Send test message |\n| `system_health` | system | CPU/Memory/Disk dashboard |\n| `quick_backup` | system | Backup agent file |\n| `skill_stats` | system | Execution stats by category |\n| `agent_uptime` | system | How long agent has run |\n| `list_files` | filesystem | List directory contents |\n\n---\n\n## 🚀 Common Skill Patterns\n\n### Pattern 1: HTTP Request Skill\n```python\ndef _skill_fetch_data(**kwargs):\n    try:\n        import requests\n        r = requests.get(\"http://example.com/api\", timeout=10)\n        data = r.json()\n        return {\"success\": True, \"result\": f\"Got {len(data)} items\"}\n    except Exception as e:\n        return {\"success\": False, \"error\": str(e)}\n```\n\n### Pattern 2: File Operation Skill\n```python\ndef _skill_read_file(path=None, **kwargs):\n    if not path:\n        return {\"success\": False, \"error\": \"Missing: path\"}\n    try:\n        with open(path) as f:\n            content = f.read()\n        return {\"success\": True, \"result\": f\"Read {len(content)} bytes\"}\n    except Exception as e:\n        return {\"success\": False, \"error\": str(e)}\n```\n\n### Pattern 3: System Command Skill\n```python\ndef _skill_run_cmd(**kwargs):\n    try:\n        import subprocess\n        proc = subprocess.run([\"ps\", \"aux\"], capture_output=True, text=True, timeout=5)\n        lines = proc.stdout.split(\"\\n\")[:5]\n        return {\"success\": True, \"result\": f\"Processes:\\n\" + \"\\n\".join(lines)}\n    except Exception as e:\n        return {\"success\": False, \"error\": str(e)}\n```\n\n### Pattern 4: Telegram Integration Skill\n```python\ndef _skill_telegram_check(**kwargs):\n    token = os.getenv(\"TELEGRAM_BOT_TOKEN\")\n    chat_id = os.getenv(\"TELEGRAM_CHAT_ID\")\n    if not token or not chat_id:\n        return {\"success\": False, \"error\": \"Set TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID\"}\n    try:\n        import requests\n        # Verify token\n        r = requests.get(f\"https://api.telegram.org/bot{token}/getMe\", timeout=5)\n        if r.status_code == 200:\n            return {\"success\": True, \"result\": \"✅ Telegram connected\"}\n        return {\"success\": False, \"error\": f\"HTTP {r.status_code}\"}\n    except Exception as e:\n        return {\"success\": False, \"error\": str(e)}\n```\n\n---\n\n## 📝 Skill Parameter Handling\n\n### Accepting Parameters\n```python\ndef _skill_process_file(filepath=None, max_lines=100, **kwargs):\n    \"\"\"Process a file with optional max lines.\"\"\"\n    if not filepath:\n        return {\"success\": False, \"error\": \"filepath required\"}\n    try:\n        with open(filepath) as f:\n            lines = f.readlines()[:max_lines]\n        return {\"success\": True, \"result\": f\"Processed {len(lines)} lines\"}\n    except Exception as e:\n        return {\"success\": False, \"error\": str(e)}\n```\n\n### Calling with Parameters\n```bash\n# Via CLI (JSON format)\n/skill process_file {\"filepath\": \"/etc/hostname\", \"max_lines\": 5}\n\n# Via code\nresult = agent.skill_manager.execute_skill(\n    \"process_file\",\n    {\"filepath\": \"/etc/hostname\", \"max_lines\": 5}\n)\n```\n\n---\n\n## 🔧 Error Handling Best Practices\n\n✅ **Good:**\n```python\ndef _skill_good(**kwargs):\n    try:\n        # logic\n        logger.info(\"Completed successfully\")\n        return {\"success\": True, \"result\": \"Done\"}\n    except ValueError as e:\n        logger.warn(f\"Invalid input: {e}\")\n        return {\"success\": False, \"error\": f\"Invalid: {e}\"}\n    except Exception as e:\n        logger.error(f\"Unexpected error: {e}\")\n        return {\"success\": False, \"error\": str(e)}\n```\n\n❌ **Bad:**\n```python\ndef _skill_bad(**kwargs):\n    # No error handling\n    result = risky_operation()  # Can crash\n    return {\"success\": True, \"result\": result}\n```\n\n---\n\n## 📊 Check Current State\n\n```bash\n# List ALL skills\nlist all current agent skills\n\n# Get stats\n/skill skill_stats\n\n# Get system health\n/skill system_health\n\n# Check uptime\n/skill agent_uptime\n```\n\n---\n\n## 🐛 Debugging\n\n### Enable Debug Logging\n```python\nlogger.setLevel(logging.DEBUG)\n```\n\n### Check Skill Execution\n```bash\n# Run and check logs\npython agent_v2.py 2>&1 | grep \"_skill_\"\n```\n\n### Verify Skill Registration\n```python\nprint(agent.skill_manager.skills.keys())\n# dict_keys(['telegram_bot_status', 'telegram_send_test', ...])\n```\n\n---\n\n## 🚨 Common Mistakes\n\n| Issue | Fix |\n|-------|-----|\n| `Skill 'xyz' not found` | Use exact name: `list all current agent skills` to see all names |\n| Function doesn't run | Make sure it's registered in `_register_agent_skills()` |\n| Returns wrong format | Always return `{\"success\": bool, \"result\": str, \"error\": str}` |\n| Timeout/hangs | Add timeout protection or break into smaller skills |\n| No env vars found | Set them: `export VAR=value` then restart agent |\n\n---\n\n## 💡 Pro Tips\n\n1. **Use categories consistently** → Makes `list all skills` clean\n2. **Always log entry/exit** → Easier debugging: `logger.info(\"skill: done\")`\n3. **Validate parameters early** → Return error dict before complex logic\n4. **Keep skills small** → One job per skill, compose in your agent\n5. **Return human-readable output** → Your LLM will thank you\n6. **Test locally first** → Run `/skill <name>` before adding to agent\n\n---\n\n**Last Updated:** 2026-04-13  \n**Status:** Production ✅\n